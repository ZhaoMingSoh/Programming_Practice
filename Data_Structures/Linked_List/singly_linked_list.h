#ifndef SINGLY_LINKED_LIST_H
#define SINGLY_LINKED_LIST_H

class Node{
    public :
    // Attributes
    int data;
    Node *ptr_next;

    // Initialisation
    Node();
    Node(int data, Node *ptr_next);
    ~Node();

    // Functions
};

#endif
